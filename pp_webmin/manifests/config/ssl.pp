##
# SSL Certificates (must be shared between usermin and webmin, as they are the same files)
# Use include pp_webmin::config::ssl
class pp_webmin::config::ssl () inherits pp_webmin {

    ##
    # Copy SSL Certificates


}